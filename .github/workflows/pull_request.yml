name: Pull request

on:
  pull_request:
    branches:
      - master

jobs:
  pull_request:
    runs-on: windows-latest
    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure node
        uses: ./.github/actions/setup-project

      - name: Lint code
        run: npm run lint

      - name: Run unit tests
        run: npm run test

      - name: Build app
        run: npm run build

      - name: Deploy Vercel
        uses: ./.github/deploy-vercel
        env:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
